import React from 'react';
import {Container, Typography} from '@material-ui/core';


export default ()=> (<Container><Typography variant="h5">The app's name is kudamono, which means fruit in japanese</Typography></Container>)